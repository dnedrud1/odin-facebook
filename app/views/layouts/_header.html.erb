<!-- Fixed navbar -->
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Friendbook", root_path, class: "navbar-brand" %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
        
          <% if current_user.friend_requests.any? %>
            <% @number_notifications = current_user.friend_requests.count %>
            <li><%= link_to "Notifications", requests_path, :data => {:toggle=>"tooltip"}, "data-placement" => "bottom", "data-original-title" => "#{@number_notifications}" %></li>
          <% else %>
            <li><%= link_to "Notifications", requests_path %>
          <% end %>
          
          <li><%= link_to "All Users", users_path %></li>
          <li><%= link_to "Profile", current_user %></li>
          <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign up", new_user_registration_path %></li>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

